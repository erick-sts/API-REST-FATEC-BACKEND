{
    "version": 2,
    "builds":[
        {
            "src": "./src/database/db.js",
            "use":"@vercel/node"
        }
        
    ],
    "routes": [
        {
            "src":"/(.*)",
            "dest": "/api/app.js"
        }
    ]
}